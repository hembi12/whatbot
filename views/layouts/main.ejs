<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Bot WhatsApp</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <div class="container">
        <% if (typeof showBackButton !== 'undefined' && showBackButton) { %>
            <a href="/" class="back-btn">← Volver al Panel</a>
        <% } %>
        
        <%- body %>
    </div>
</body>
</html>